# Equity-OS blueprint-completion — session handoff (2026-08-19)

Read this first, then `CLAUDE.md`, `AGENTS.md`, `CONTEXT.md`, `.claude/project/*`. Run `bd prime`.

## 1. Goal and the three standing rules
- Goal: `docs/goals/equity-os-blueprint-completion.md` (the contract; three validators are EXTRACTED from it — amending a validator = amending the goal; `extract_goal_validators.py --check` must stay exit 0).
- Roles (CONTEXT.md "Agent roles"): Orchestrator = main session; Implementer = Claude Opus 5 medium; Reviewer = Claude Opus 5 high, always an independent agent per artifact. Never hard-code vendor lanes in docs. **gpt-5.6 (sol/terra/luna) and Codex CLI are prohibited** (one-time HR-0004 exception already spent).
- **User gate (bead eqos-jce):** no product code before the user approves the architecture artifact. Artifact v1 published 2026-08-17: https://claude.ai/code/artifact/306257cd-381e-4421-896c-f83f3e4c593d — source `docs/goals/architecture/equity-os-architecture-of-record-v1.html`, brief `docs/goals/architecture/architecture-brief-v1.md`. **NOT yet approved.** User asked for two additions before approving: (a) a "day in the life" operational workflow section + toolchain table (the text was given in chat; reconstruct from the brief §5 + spec S09/S11/S14/S15), (b) a "Data sourcing strategy" item: proposed 3-tier = official XBRL first → PDF extraction for the gap → licensed vendor feed only as a reconciliation cross-check if S02 rights allow; marked as the user's decision under A-05/A-06. No library/vendor is chosen today; GBrain is NOT used and is explicitly not a dependency (DEF-02).

## 2. Canonical state (all committed on main)
```
goal            b77ea73d90fb6f7499a7fcf74f50c471bb03dd5a6f1bf0c71f510af917d0b0c9
ledger          af4f3af41ef019807e3081ba46e0b391d5b0ba532447b3b80299b4c799dd2237
human-review    51bc4f9afa8a3e3478affc5452118e7dc71dd3e3b28568b4faabcbcd2a72a9ce
structural      77faeaf3dee13d5d2bfb50c255b054cde94ef0118751b247e23248e343964fff
recorder        94c65444cda07978ecef4ec7b6a241e4dab2f62677795bdfc0c718a821423341
```
- HR-0004 and HR-0005 executed (user-approved, byte-verbatim questions; evidence in `docs/goals/handoff/hr0005-*.md`, `scratchpad/hr0004/`, `scratchpad/disp-r1/exec/`).
- Inventory reviews: 444/447 artifacts written by independent Reviewer agents; **337 recorded** (pending 447 → 110, stale 0, structural 0). Recorder design lineage: `docs/goals/reviews/ledger/equity-os-blueprint-inventory-review-recording-design-r{2..6}.md` + reviews; r6 is CLEAN and authoritative.
- Gate: `python3 scripts/equity_os_blueprint/validate_ledger_preimplementation.py --repo-root . --report-blockers` → exit 2, ready=false, 110 pending.

## 3. IMPORTANT working-tree fact
The 36 still-unrecorded components' artifacts under `docs/goals/reviews/ledger/inventory/{DISP-*,REG-A-09,SEQ-02,SEQ-03,SEQ-04,SEQ-08}/` are **intentionally untracked** (recorder design r6 §3.8 step 8 requires a batch's artifacts to be untracked at record time and committed together with the ledger change). Do NOT bulk-commit them; do NOT delete them. They get committed per batch by the recording script.

## 4. Recording workflow (proven)
```
bash scripts/equity_os_blueprint/record_inventory_batch_by_kind.sh <kind>
```
generates a manifest (excludes non-CLEAN components and DISP-R-1), dry-checks, records for real, runs the structural validator, and commits ledger + that batch's artifacts. Non-clean artifacts abort a batch by design (§5.4).

## 5. What is pending, in order
1. **USER decisions:** (a) approve architecture artifact (after additions in §1); (b) RC-2 policy — multi-spec rows: one approval covers all applicable specs, or one per spec? (affects SEQ-02/03/04/08 + 16 disposition rows); (c) data-sourcing direction (§1b); (d) `git push origin main`.
2. **RC-1** (see `docs/goals/handoff/nonclean-review-triage-2026-08-17.md`): 10 disposition rows' EVIDENCE reviews say `required_evidence` lacks `REQ-<CID>-SPEC-REVIEW`; reviewers claim all 32 disposition_item rows share it (22 were reviewed CLEAN → contradiction to resolve first). If real: ledger edit on 32 rows (inside review_input_projection → must land BEFORE recording those rows; likely a journaled transaction at the HR-0004 bar with design + independent review), then r1 re-reviews of affected rows. All 32 disposition rows (96 reviews) are HELD for this.
3. **RC-3** DISP-M-3 missing human approval entry (+ TYPED_APPROVAL evidence); **RC-4** REG-A-09 missing PRODUCT_OWNER_DECISION. Ledger edits, then r1 reviews.
4. **DISP-R-1 T2**: the current no-implementation proof (now possible post-HR-0005; two-state rule in structural validator `EXPECTED_DISP_R1_REQUIREMENT_IDENTITY`/`DISP_R1_MUTABLE_FIELDS`). Per disp-r1 design r3 §8.3: T2 runs last and re-seals DISP-R-1's three reviews; recorder refuses DISP-R-1 once SATISFIED (`DISP_R1_RESERVED_FOR_T2`). Batch 6 (DISP-R-1 alone) never ran.
5. **25-spec closure** (`docs/goals/handoff/spec-closure-audit-r0.md`): 22 specs need DELEGATED_ARTIFACT_APPROVAL records written into the ledger (123 requirements UNRESOLVED, 0 records); S06/S09/S10 gated on HR-0001/0002/0003, whose text must first be reworded out of vendor-lane terms ("Sol xhigh") into role terms, then asked of the user byte-verbatim. Unsettled: whether HR-0004's changes stale the 22 prior approvals.
6. TERM-0001 (eqos-sky): 123 lane-bearing ledger rows.
7. Then gate → ready=true → (with architecture approval) implementation.

## 6. Beads
`bd ready`; key: eqos-jce (architecture gate), eqos-sky (TERM-0001); eqos-w78 closed. Memories: `bd memories` (agent policy, architecture gate).

## 7. Scratchpad (gitignored, same machine only)
`scratchpad/continue.txt` (checkpoints 1–11), `scratchpad/w78/` (recorder rounds, reviews' probes), `scratchpad/disp-r1/` (amendment rounds + exec), `scratchpad/arch/`, `scratchpad/inventory-reviews/` (rehearsal proof the recorder's gate requires — keep it).
