
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.pyc
scratchpad/
# Retained source content — internal-only rights, never commit/redistribute
data/

# Fundamentals validation machine reports (large, regeneratable; keep .md rollups)
docs/research/validation/*.json
docs/research/validation/**/*.lock

# Python bytecode caches
__pycache__/

# Codex CLI session artifacts
:memory:*.ses
