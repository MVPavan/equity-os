# Codex Migration Notes

This file was generated by `$migrate-claude-to-codex`.

Review the migrated assets before treating them as semantically equivalent to the Claude Code harness.

## Mechanical Mapping

- `.claude/skills/` -> `.codex/skills/`
- `.claude/commands/` -> `.codex/skills/`
- `.claude/agents/` -> `.codex/agents/`
- `.claude/project/` -> `.codex/project/`
- `.claude/docs/` -> `.codex/docs/`
- `.claude/rules/` -> `.codex/rules/`
- `.claude/hooks/` and `.claude/settings.json` -> `.codex/hooks/` and `.codex/hooks.json`

## Manual Review Applied

- Replaced the Claude-side Codex adapter workflow with native Codex routing.
- Reframed Agent Matrix and model-council routing around the live Codex spawn contract; the repository Matrix remains unavailable until its catalog and validator are added.
- Corrected custom-agent sandboxes and removed the unavailable Context7-only dependency.
- Preserved the `.agents/skills/beads` installation as canonical instead of exposing a conflicting duplicate.
- Merged reviewed safety hooks into the existing Beads-generated hook file and adapted file-edit policy to Codex `apply_patch` payloads.
- Converted command-skill references to `<skill>/SKILL.md` and Codex `$skill-name` invocation syntax.
- Validate the final result with hook fixtures, exec-policy checks, skill validation, the migration verifier, and live Codex discovery.

Historical Claude-specific reference docs and intentionally cross-provider skills may still discuss Claude. They are not authoritative for native Codex execution; `$use-codex` is.
